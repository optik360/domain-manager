<%= simple_form_for client do |f| %>
  <%= hidden_field_tag 'redirect_path', params[:redirect] %>
  <%= f.input :name %>
  <%= f.input :contact_info %>
  <%= f.submit "Envoyer", class: "btn btn-primary" %>
<% end %>