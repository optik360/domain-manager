<button class="btn btn-lg btn-default"><%= link_to "ajouter", new_client_path %></button>
<table id="table" class="display" cellspacing="0" width="100%">
        <thead>
            <tr>
                <th>Nom</th>
            </tr>
        </thead>
        <tfoot>
            <tr>
                <th>Nom</th>
            </tr>
        </tfoot>
        <tbody>
            <% clients.each do |c| %>
            <tr>
                <td><%= link_to c.name, client_path(c) %></td>
            </tr>
            <% end %>
        </tbody>
    </table>